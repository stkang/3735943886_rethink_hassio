# CST_1A4001_WW 지원 추가

## 요약

`CST_1A4001_WW` 에어컨을 기존 `CST_170004_WW` 구현과 분리해 모델별 기능으로 지원한다.
기존 모델 파일은 수정하지 않고, `CST_1A4001_WW` 클래스에서 Home Assistant 노출 방식과 패킷 처리를 오버라이드한다.

## 변경 배경

LG ThinQ 앱에서 확인한 `CST_1A4001_WW`의 기능 구성이 기존 CST 모델과 달랐다.

- 회전: 정지/상하/좌우/자동이 아니라 상하 회전 `켜짐/꺼짐`
- 수평 회전: 정지 및 1~6단이 아니라 수평 회전 `켜짐/꺼짐`
- 디바이스 밝기: 밝기 단계가 아니라 `켜짐/꺼짐`
- 바람 모드: 지원하지 않음
- 쾌적 절전: 지원하지 않음
- 프리셋: 지원하지 않음
- 자동 건조: AI 자동 건조 스위치와 건조 시간 선택을 별도 기능으로 제공

## Home Assistant 엔티티

### Climate

- `swing_mode`: `on`, `off`
- `swing_horizontal_mode`: `on`, `off`
- 프리셋 미노출
- 모델에서 지원하지 않는 바람 모드 및 쾌적 절전 미노출

### 기기 설정

- `디스플레이`: 스위치
- `자동 건조`: 스위치
- `자동 건조 시간`: 선택 엔티티
    - `자동`
    - `30분`, `40분`, `50분`, `60분`, `70분`, `80분`, `90분`, `100분`

`자동 건조 시간`은 `entity_category: config`로 등록해 기기의 설정 영역에 표시되도록 했다.

## 패킷 매핑

| 기능           |     TLV | 값                   |
| -------------- | ------: | -------------------- |
| 상하 회전      | `0x205` | `1`/`0`              |
| 수평 회전      | `0x206` | `1`/`0`              |
| 디스플레이     | `0x21f` | 켜짐 `12`, 꺼짐 `11` |
| AI 자동 건조   | `0x20e` | 켜짐 `255`, 꺼짐 `0` |
| 자동 건조 시간 | `0x0f4` | 자동 `2`             |
| 자동 건조 시간 | `0x0e7` | `30`~`100`분         |

자동 건조 시간은 `autodry_setting`에 직접 쓰기 처리를 구현해, 읽기 전용 필드로 잘못 인식되던 `Attempting to set property autodry_setting which is not writable` 오류를 방지한다.

## 구현 범위

- `cloud/devices/CST_1A4001_WW.ts`
    - 모델별 팬 레벨 및 엔티티 구성
    - 독립 회전 제어
    - 디스플레이 스위치 변환
    - AI 자동 건조 및 시간 선택
    - 미지원 기능 제거
- `tests/cloud/devices/CST_1A4001_WW.test.ts`
    - 상태 디코딩
    - 패킷 쓰기 검증
    - Home Assistant discovery 구성 검증

`CST_170004_WW.ts`에는 변경사항이 없다.

## 검증

- 모델 테스트: 6개 통과
- TypeScript 빌드: 통과
- `git diff --check`: 통과

기존 Home Assistant discovery 정보가 남아 있는 경우, 기기를 삭제 후 다시 검색해야 변경된 엔티티 구성이 반영된다.
